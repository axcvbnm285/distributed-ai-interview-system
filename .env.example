PUBLIC_FRONTEND_URL=http://localhost:8080
FRONTEND_PORT=8080

# Optional build-time frontend overrides.
# Leave these blank when using the bundled Nginx proxy in docker-compose.
VITE_BACKEND_URL=
VITE_API_URL=
VITE_SOCKET_URL=
